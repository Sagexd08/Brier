[workspace]
resolver = "2"
members = ["programs/*", "sp1-calibration/lib"]

[profile.release]
overflow-checks = true   # Rust wraps in release by default; Solidity 0.8 reverts.
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
